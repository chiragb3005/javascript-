// Destructuring
// method so we dont have to call an object again n again 
// will do same in React

const course = {
    courseName : 'JS in hindi',
    price: 1000,
    instructor: 'hitesh'
}
// course.instructor

const {instructor} = course
console.log(instructor);


// later we have to go to API and debug them....... JSON

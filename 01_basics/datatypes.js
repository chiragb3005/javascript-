"use strict"; 
//will strictly use the newer version of js not the older one, it will throw an error if we use any of the older version of js features.

// alert(3+2) // using nodejs not bowser js

let name = "chirag"
let age = 20
let isLoggedIn = true
let state = null

// strign => ""
// boolean => true/false
// null => stand alone value which is not assigned to any variable, it is an empty value
// undefined => variable is declared but not assigned any value to it, it is also an empty value
// symbol => unique value
// object =>

console.log(typeof name) // string
console.log(typeof age) // number
console.log(typeof isLoggedIn) // boolean
console.log(typeof state) // null
let index = 1
while (index<11) {
    // console.log(`value of index is: ${index}`)
    index = index+2
}

let myArray = ['flash', 'batman', 'sueprman']
let arr = 0
while(arr<myArray.length) {
    // console.log(`value inside array: ${myArray[arr]}`)
    arr++
}

// do-while loop

let score = 11
// loop will run atleast one time atleast

do{
    console.log(`score is: ${score}`)
    score++
} while (score<11)